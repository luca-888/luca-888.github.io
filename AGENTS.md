# 项目约定

## 项目方向

- 站点名称为 `luca’s blog`，页头、页脚及浏览器标题保持一致。
- 本仓库用于持续发布个人技术博客，主题包括机器学习、系统实现与性能优化。
- 本文件只记录适用于多篇文章的共享约定；单篇文章的大纲、参数、示例和实现细节放在对应内容或代码中。
- 正文默认使用简体中文，专业术语优先使用通行英文，避免生硬翻译。同一概念的叫法、符号和单位在文章内保持一致。

## 技术栈与部署

| 用途 | 技术 |
| --- | --- |
| 页面与交互 | React + TypeScript |
| 开发与静态构建 | Vite |
| 样式 | 原生 CSS |
| 数学公式 | KaTeX |
| 代码高亮 | Shiki |
| 数据图表 | Apache ECharts，按需使用 |
| 托管与部署 | GitHub Pages + GitHub Actions |

- 页面功能须适用于静态托管；Markdown、公式与代码高亮由现有构建流程处理。
- 部署工作流位于 `.github/workflows/pages.yml`，由 `main` 推送或手动触发，部署 `dist/`。
- 仓库名为 `luca-888.github.io`，站点地址为 `https://luca-888.github.io/`。Vite 的 `base` 为 `/`，静态资源和链接从站点根路径加载。
- 提交依赖锁文件，构建命令包含 TypeScript 类型检查。

## 内容组织

- 文章目录统一维护于 `src/posts.ts`，主题筛选从数据生成。
- 每篇文章使用稳定的 `slug`，正文放在 `content/`，页面链接为 `/posts/<slug>/`。添加文章时登记目录数据、页面入口及 `vite.config.ts` 的构建入口。
- 复用 `build/markdown.ts` 的 Markdown、公式、代码高亮和锚点处理，以及共享的文章排版样式。
- 草稿使用 `draft` 状态并明确标注；发布时填写真实的 `publishedAt`（`YYYY-MM-DD`），按日期倒序展示，不虚构文章或日期。

## 页面与交互

- 首页直接展示文章目录、筛选和搜索，不添加抒情大标题、口号或装饰性介绍。
- 正文居中单栏，不设置左侧目录或常驻章节导航。保持留白、清晰层级、细分隔线和低饱和度配色。
- 当前优先实现与验证电脑端，不额外投入移动端适配或浏览器兼容工作。交互支持键盘操作。
- 使用简洁的明暗模式开关：首次跟随系统，手动选择后记住偏好，跨页保持；正文、公式、代码、图表使用一致主题。
- 主题初始化脚本使用 `/theme.js`，由 Vite 按 `base` 处理部署路径，不手动拼接 `%BASE_URL%`。
- 可视化服务于理解具体问题。控件保持精简，明确数值范围、单位和刻度；动态数值预留宽度，避免换行和布局跳动。
- 示例数据明确标为示意；真实数据注明来源。参数和参考来源用简短的辅助说明呈现，不挤占正文主线。

## 技术文章写作

- 内容精炼、逻辑完整。每章解决一个明确问题，每段增加新信息；删除重复定义、公式、逐句复述、无信息量的过渡和总结。
- 标题简短、具体，直接说明主题，不使用抒情、宣传或悬念式表述。
- 优先通过必要的公式、代码、表格或可视化建立直觉。已讲清的内容直接引用，不反复铺垫，不套用固定的章节模板。
- 数学表达使用清晰的符号与常用缩写；首次使用时定义含义。描述数据结构、维度和计算范围时给出明确标记，避免仅靠方位或模糊措辞。
- 精简不能省略影响结论的必要假设、适用范围、数据类型、精度语义或边界条件，在相关位置简短说明。
- 区分事实、推导、预期与实测。技术结论提供相应证据，引用优先使用原始论文、官方文档或具体源码。

## 代码与性能分析

- 实现章节以代码和关键设计选择为主。标为完整或可运行的示例须包含必要依赖、调用方式和验证；局部片段明确其上下文，不用省略号冒充完整实现。
- 短调用优先放一行，仅在过长或影响阅读时换行，不机械地逐参数拆行。
- 解释为什么这样实现、解决什么问题及代价。代码直接可见的语法和赋值不逐行翻译，注释与正文不重复。
- 分析优化时围绕“现象 → 改动 → 代价 → 测量结果”展开，展示关键代码差异。参考其他实现时指出具体设计选择，不罗列工具或整份搬运源码。
- 性能结果注明 workload、环境、版本与测量方法；比较相同工作量和语义。用 profiler 证据解释瓶颈，用独立 benchmark 报告性能，不把预期或未运行的测试写成已验证。

## 开发与验证

- 提供 `npm run dev`、`npm run build`、`npm run preview` 命令。
- 日常文案和样式调整使用开发预览自动更新，检查实际显示，无需每次 build。阶段完成或发布前统一构建。
- 修改计算或交互逻辑时做相应的数值或交互验证；修改 Markdown 渲染流程时运行 `npm test` 和 `npm run build`。
- 检查桌面布局及部署路径下的资源加载。修改主题机制时验证开发预览和生产构建中的跨页保持。
- KaTeX 渲染器与浏览器样式必须使用同一版本；升级时检查 `npm ls katex`，并实际查看分数、根号、求和、多行公式和方框的排版。

## Markdown 加粗约束

- 中文短语加粗时，把末尾冒号、逗号等放在标记外：写 `**算尺度**：正文`，不要写 `**算尺度：**正文`。标记内侧不要留空格。
- 整句加粗后接正文时，留空格或换行：`**完整的一句。** 下一句`。
- 展示字面 `**` 时使用行内代码、代码块或显式转义。
- `build/remark-bold-guard.ts` 检查正文中未解析的 `**`；不得关闭检查或批量替换渲染后的 HTML 来绕过问题。
