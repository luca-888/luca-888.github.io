# 项目约定

## 项目方向

- 本项目是个人技术博客，用于分享技术文章、学习笔记与实践经验，内容不限于机器学习或算子优化。
- 首篇文章主题是 RMSNorm 的计算原理与 GPU 算子优化；它是博客中的一个主题，不限定整个站点的内容范围。
- 页面内容默认使用简体中文，技术名称与代码保留原文。
- 按用户要求逐步实现，围绕已有文章与明确需求完善页面和交互。
- RMSNorm 文章按 PyTorch reference、eager 性能分析、torch.compile、Triton 优化、结果总结展开；当前手写 GPU kernel 仅使用 Triton。
- RMSNorm 当前仅讨论 BF16 输入与输出，保留 FP32 中间计算；优化与性能对比同时考虑延迟和峰值显存增量。

## 写作与代码风格

- 正文代码只保留理解实现所需的部分，省略 `import`、环境搭建和运行入口，突出核心逻辑。
- 简短的函数调用和赋值尽量写在一行；参数较少、表达清楚时不拆行，避免仅为格式化增加代码块高度。
- 围绕当前需求实现，避免过度扩张、过度抽象和不必要的防御性编码，保持代码简洁、清晰、优雅。
- 该用术语时使用准确、通行的英文术语，避免生硬翻译；必要时用简短中文解释，保持行文自然、易读。
- 正文优先直接解释概念、机制与结果，少用“并不代表”“不能据此推断”等防御性补充。会影响理解或结论的适用条件、证据缺口可就近简要说明，避免在多处重复提醒，让行文围绕主线自然展开。
- 章节标题使用简洁、正式的技术表述。
- 正文中的测试环境最多注明显卡型号，不展开显存容量、软件版本、驱动、CPU、虚拟化环境、测试日期或频率设置；详细环境保留在原始测量记录中。
- 正文不写 benchmark 规则、计时口径或操作流程，包括预热、同步、重复次数、统计方式、缓存设置及 profiler 采集步骤；直接展示数据与分析，测量细节保留在原始记录中。
- RMSNorm 第一章以计算图、梯度流和参考实现为主，减少逐步求导与重复说明。
- 同一计算量在正文、计算图和参考代码中使用一致的变量名；第一章统一使用 x、gamma、s、r、x_hat、y、g、dx_hat、dr、dx、dgamma。

## 配色规范

- 全站支持浅色与夜间模式：默认跟随系统，页头按钮手动切换并记住选择。下述白底规范适用于浅色模式；夜间模式使用 `src/styles.css` 的深色变量，正文、表格、自绘图与代码高亮同步切换，官方原图保留原色。新增页面在 HTML head 中加载 `/theme.js`，并复用公共页头。

- 站点 UI 使用白色与中性浅灰作为底色，深灰文字、蓝灰主色、灰绿辅助色；背景、边框和辅助文字不偏黄绿。正文表格与技术图以理解效率和视觉层次为先，不受 UI 的素淡风格限制。
- 公共颜色复用 `src/styles.css` 的 CSS 变量：标题 `--color-heading: #24282b`、正文 `--color-text: #393e42`、辅助文字 `--color-muted: #73797e`、面板与代码底色 `--color-surface: #f7f8f9`、边框 `--color-border: #e1e4e7`；页面背景为 `#fff`。
- 站点 UI 默认用蓝灰 `#567891` 主要强调、灰绿 `#587767` 辅助对照，输入输出交互分别使用这两色。表格与技术图可根据语义采用更鲜明的配色与更强的明暗对比，不必全部使用浅色变体。
- 表格可用有色表头、分组底色、交替行底色、关键行列和数值强调建立层次，不要求透明表头或一律素淡；分隔线、颜色与留白共同帮助比较和定位信息。
- 表格与技术图不限定固定色表；颜色用于区分阶段、类别、状态或突出关键关系，同一图表中的颜色含义保持稳定，并配合文字、位置或形状表达，不能只靠颜色辨识。

## 表格与可视化

- 表格与技术图以帮助读者快速理解为首要目标：第一眼看到核心关系与结论，其次看懂执行顺序、分组和层次，最后再阅读变量、精度等细节。美观与整体风格服务于理解，不以“素”“低饱和”作为硬性目标。
- 参考 [OpenAI 官方图表实例与观察](docs/visual-references.md)：先用构图、对齐、留白、字号和字重建立主次，再对当前对象或关键关系作局部色彩强调。保持文字清晰；放宽风格不等于默认铺大面积饱和色块或使用白字彩色表头。参考实例而非照搬，不将单篇文章的样式当作统一官方规范。
- 自绘图先确定要解释的核心问题，再安排主次。允许鲜明色块、强对比、大字号、较粗的关键箭头、局部深色背景和必要的空间层次；用这些手段引导阅读，不堆叠无关装饰。
- 不把每个变量都画成同等大小、同等强调的小框。主流程与关键结果应显著突出，辅助状态、存储位置、精度和源码名可分层标注或移到图旁说明；必要时拆图或使用逐步交互，不在一张图中塞满所有细节。
- 有适用的官方图时，优先使用官方原图并注明来源，保留原有标签、配色与连接细节。
- 从官方页面、PDF 或源文件取得高分辨率原图，不把用户提供的压缩截图或网页缩略图当作原图。
- 仅在没有适用官方图，或需要解释官方图未覆盖的关系、展示实测数据时自绘图形。
- 官方原图不强制改色或套用本站自绘图形规范；下述技术图配色与 SVG 样式约定适用于本站自绘图形。
- 表格的表头、数据分组、对照项和关键结果应有清楚的视觉层次，颜色、字重与边线强度按内容选择；不为追求简素而削弱可读性，也不把所有单元格同等强调。
- 所有表格复用 `src/styles.css` 中的 `.table-scroll` 基础样式，包括 Markdown 表格；基线表的分组表头使用 `.benchmark-table` 扩展，避免各表重复定义基础样式。
- 表格整体居中，宽度随内容，默认单元格 padding 为 `7px 16px`、行高为 `1.5`；可按信息密度调整，不为了紧凑而挤压内容。
- 表格优先通过对齐、分组底色与分隔线组织信息，必要时可用竖线或边框区分数据组；表头可加底色、提高字重与字号。正文默认 13px，按实际阅读尺寸调整，不固定所有表头为 12px。
- 数字及数值表达式右对齐，文本列左对齐，表头与对应数据同向对齐；数字使用等宽字体和 `tabular-nums`，同列保留一致的小数位数。
- 多指标表格按耗时、显存等分组，组标题居中，单位只在表头显示一次；表内代码不加灰底胶囊，短说明贴近表格且保持简短。
- 计算图通常使用 React + SVG，可结合 HTML/CSS 与必要的交互；性能图表使用 ECharts，优先 SVG renderer。工具与布局根据表达需求选择，图例、控件与结果靠近图形。
- 图中文字大小、字重、线宽与色块强度按信息层级选择，不固定为统一的小字号和细线。关键运算、阶段与结果应明显大于辅助信息；源码变量与代码使用等宽字体，数学符号使用清晰的数学排版。
- 今后正文、表格、自绘图及图注中的数学表达式统一使用 KaTeX，包括上下标、转置、重音符号、分式和求和等；不再用 Unicode 组合字符、普通文本或手工定位拼接公式。SVG 中通过 `foreignObject` 或配套 HTML 容器嵌入 KaTeX，为重音与上下标预留空间。源码标识符保留等宽字体，引用的官方原图保留原样。
- SVG 节点、箭头与标签应对齐，文字不遮挡、不裁切；通过布局和节点尺寸容纳内容，不靠缩小整图让文字难以阅读。关键变量与结果直接可见，详细算子信息同时提供可读文本，不只依赖悬停。

## 首页与文章头图

- 封面生成先查 [封面风格选择库](docs/cover-styles/README.md)，按编号选择参考图与可复用 Prompt。当前收录 S01 浅色概念图、S02 官方硬件原图配大字；风格可轮换，构图随主题变化。已确认风格与新试稿分开记录，不把所有文章套成同一版式。
- 首页主题图与文章头图应高清、抓眼，具有科技感、空间层次和精细质感；文字与主题图形共同组织构图，比例按实际展示位置确定。
- 文字是主视觉元素之一，采用素净、清晰的平面大字，以字号、字重、留白和对比建立视觉分量；不要求居中。默认不加金属材质、立体挤出、倒角、发光或强透视，保证准确拼写与完整轮廓。
- 不同文章应适度变换封面的配色、构图与图形语言，避免只替换文字和图形、反复套用同一个模板。某篇获认可的封面是该篇的方向，其他文章结合主题与用户提供的参考图另作设计。
- 硬件画面采用两条明确路线：使用真实设备照片或官方高质量产品渲染；或者制作清楚表达主题的概念图。具体服务器、GPU、板卡的实物结构优先使用可靠原图，不用 AI 仿造细节不足、接口和结构似是而非的写实硬件。
- 概念图适用于软件及硬件主题，从计算、矩阵、分块、连接、调度或数据流等核心机制提炼造型。形体与关系应明确，材质和布光精细，避免无意义的抽象装饰、发光管线、石材场景和无关环境。
- 软件算法封面不沿用硬件产品的厚重金属渲染；优先用计算关系、执行轨迹、平面色块或半透明叠层表现主题与层次。新风格仍需通过样稿确认，不将一次尝试自动记为已认可的规范。
- 视觉冲击力主要来自主体造型、构图、尺度对比、光影与前后层次；可以采用深黑或石墨色背景与克制的色彩强调，不靠堆叠特效或厚重金属字制造科技感。
- 已确认的整体参考是 [RTX PRO 6000 多卡拓扑：官方素材排版稿](output/imagegen/hero-direction-v2/01-official-source-layout.png)。用户明确认可此图：素净平面白色大标题、深色背景、主体清晰且细节精细的官方硬件素材。后续硬件头图优先沿用该方向；[来源与提示词](output/imagegen/hero-direction-v2/prompts.md)一并保留。该图是基于 NVIDIA 官方渲染的编辑排版稿，不作为实测节点照片或精确拓扑证据。
- RMSNorm 已确认选用 [浅色矩阵封面](output/imagegen/rmsnorm-cover-styles/02-light-matrix.png)：浅底、素净黑色大字、蓝橙矩阵与行内计算概念图；[提示词](output/imagegen/rmsnorm-cover-styles/prompts.md)一并保留。这是该篇文章的选稿，可作为软件主题封面的参考，不意味着所有文章统一改为浅色。
- 本节适用于独立的首页主题图与文章头图；站点 UI 保持简洁，正文表格与技术图遵循“表格与可视化”中的理解效率与层次优先原则，小红书图片按下节的独立规范制作。

## 小红书图片输出规范

### 封面

- 默认使用竖版 3:4，建议尺寸为 1080 × 1440；可按内容调整分辨率，保持标题在缩略图中清晰可读，交付独立 PNG 图片。
- 采用文字主导的技术文章封面：纯白底、深灰大字、少量低饱和图形。标题是第一视觉重点，图形只辅助表达文章主题。
- 标题左对齐，使用粗体无衬线字体，按语义分成 2–3 行，主要占据画面上部与中部。核心命题字号最大，后续标题略小；保持舒展的行距和安全边距，不裁切文字。
- 标题文字默认使用深灰 `#24282b`，可将一个关键术语用蓝灰 `#567891` 强调；辅助图形使用同系浅色与灰绿 `#587767`，沿用博客配色。
- 封面不加小字、署名、站点名、页眉、页码、摘要、口号或额外术语标签；仅保留用户确定的标题文字。
- 修饰图形集中在画面下部，与标题留出间距。使用可辨识的扁平 icon、少量节点和细线箭头，表达主题中的关系或流程；图形从属于标题，不堆叠无关装饰。
- 图形保持简洁、轻巧，允许浅色块和小圆角；避免 3D、渐变、发光、阴影、厚重边框、高饱和色和复杂电路背景，不添加浏览器外框。
- 已确认的版式示例为三行标题「压缩即智能 / 从大模型 / 到 Harness」，其中 Harness 用蓝灰色；下方以数据汇入模型、模型连接工具与反馈回路作修饰。后续封面复用这种视觉层级，图形随文章主题变化。
- 生成后核对标题文字、英文拼写、换行及边距，确认图形未抢占标题的视觉重点。以上大字与无小字约定仅适用于封面，正文阅读页另按内容排版。

### 正文阅读页

- 采用电脑端博客正文截图式排版，默认输出 1080 × 1440 的竖版 PNG；纯白底、左对齐文字，不添加浏览器外框或海报式装饰。
- 沿用已确认的第一章阅读页风格：左右边距约 72px，顶部约 62px；标题约 56px、600 字重，正文约 40px、1.68 行高，段间距约 30px。字体优先使用系统无衬线与 PingFang SC。
- 页眉左侧为文章标题、右侧为 `luca’s blog`，使用约 26px 的辅助灰文字，下方加中性细分隔线。页脚显示当前章节和连续页码，以细线与正文分隔。
- 不在底部另加“参考”“引用”等来源行；原文中的出处链接、人物归属和引用内容保留。链接使用蓝灰色，引用采用蓝灰文字与细左边线，重点只做适量加粗。
- 按语义和完整段落分页，每页可用简短小标题概括本页内容。保留原文顺序和信息，不为凑页数删减正文，不拆开引用的归属与引文，不让标题独占一页。
- 正文插图保留原比例并与对应段落就近排版，图注使用较小的辅助文字；图文页可适度调整字号与间距，但不能以缩小整页来容纳溢出内容。
- 导出前检查每页文字、插图与页脚的边界，避免遮挡、截断和不自然的分页。按阅读顺序命名独立 PNG，并提供仅包含成品图片的 ZIP；排版源码另行保留以便修改。
- 以上为图片交付规范，与博客站点的移动端适配无关；封面仍遵循上一节的独立规范。

### 生成与交付流程

- 用户要求“生成小红书图片”时，默认按以上规范完成封面与全文正文阅读页；若明确限定章节、正文或封面，则按指定范围生成。
- 从当前文章读取正文和配图，按语义安排分页，使用 HTML/CSS 排版并由浏览器导出正文 PNG。优先使用用户提供或已确认的封面原图，复制到交付目录；只有没有可用封面或用户要求重做时，才按封面规范生成。
- 检查内容完整性、页面溢出、文字与插图清晰度，生成整套预览，将成品图片按封面、正文顺序打包为 ZIP，保留可修改的排版源码。封面命名为 `00-cover.png`，正文依次编号。
- 默认交付必须包含封面与全部正文。发送前实际检查 ZIP 文件清单，确认封面位于首位、正文齐全且总数为封面加正文；不能只在邮件中注明“不含封面”就将不完整图片包作为完成交付。只有用户明确要求仅发正文时，才省略封面。
- 除在对话中提供图片包下载链接外，默认通过已连接的 Gmail 将 ZIP 作为附件发送给当前账号本人；这是用户指定的交付方式，无需每次重复确认。收件地址从当前连接读取，不在仓库中保存个人邮箱地址。
- 邮件注明文章标题及“1 张封面 + N 张正文”的数量，附件必须使用核对过的完整 ZIP；仅在发送工具返回成功后告知已发送。若连接失效或附件发送失败，保留本地交付，并说明未发送成功，再协调其他渠道，不自动发给其他联系人。

## 硬件需求与获取

- 每次开始新文章，第一步必须确定硬件需求与获取途径，优先于正文撰写、代码实现和实验开展。先判断文章是否需要硬件实验；需要时，明确所需配置并与用户确认获取途径后再推进，不需要时明确说明无需专用硬件。
- 任务涉及 GPU 或其他专用硬件时，先根据文章主题与实验目标明确所需硬件，包括架构特性、显存和设备数量等必要条件。
- 新任务或空上下文中尚未确认硬件资源时，必须先向用户说明硬件需求，并与用户确认获取途径，再执行依赖该硬件的实验。用户会提供可用硬件，不得因本地缺少设备或尚未发现访问方式，擅自降级硬件、缩减实验范围或用模拟数据替代实测。
- 已知用户有租用 RTX 4090 的资源和 Modal 账号，可通过 Modal 获取 H100、B200 等 GPU；这些是获取途径的线索，具体型号与使用方式以用户在当前任务中的确认为准。
- 当前会话已经明确确认的硬件与获取途径可直接沿用，无需重复询问；硬件受限或需求发生变化时，先与用户协商，不自行替换为较低配置。

### 高价值 GPU 与多卡实验

- 使用高价值 GPU（如 H100/H200、B200/B300、RTX PRO 6000）或任何多卡配置前，必须先制定一次性的完整测试计划，写入当前任务的实验记录，并向用户说明预计时间与花费，再申请和执行。首次设备探测、拓扑检查和试运行同样纳入计划，不以“先试一下”为由提前占用 GPU。
- 计划列明实验目标、GPU 型号与数量、获取途径、容器数量与并发、完整测试矩阵、依赖与输入、正确性检查、预期输出和完成条件；尽量在申请前完成代码、镜像依赖与无需目标 GPU 的验证，将同一硬件上的已准备测试集中在一次申请和一次远端调用中完成。
- 时间预估区分准备/构建、排队、GPU 实际占用与结果回传，给出预计总时长和 GPU 占用上限；费用按执行前查询的当前费率、卡数与预计计费时长计算，并计入 CPU、主机内存及适用的存储等费用，注明币种、估算范围、预算上限和不确定项。无法确定的费用或等待时间明确标注，不当作零。
- 执行配置必须对应计划中的超时、容器上限、并发和重试限制；异常或触及时间/预算上限时停止后续测试并保存已有结果，不自动反复申请或重跑整套矩阵。补测前更新剩余测试、预计时间和费用；超出已授权硬件、范围或预算时先与用户协商，已有授权范围内不重复询问。
- 测试完成或中止后立即释放资源并核查运行状态，记录实际占用时间与可获取的费用；账单尚未结算时明确区分估算和实际费用。写作、分析与等待用户回复期间不保持无必要的 GPU 占用。

## Modal 调用入口

- Modal 主要用于短期调试、最终验证及国内难以获得（用户所称“禁售”）的高级 GPU / 特定架构实验；具体型号、地区与账号可用性以供应商实际支持为准。
- 尽量先准备好全部实验代码、依赖与测试矩阵，将同一硬件上的正确性、性能、显存和拓扑检查合并为一次运行，复用镜像与容器，测完立即结束，减少反复启动和空转费用。
- 需要长期在线、反复交互或多轮调试时，向用户申请常驻 RTX 4090 等合适资源；能在常驻卡完成的通用调试先在那里完成，只有目标架构特有部分转到 Modal，不用 4090 替代必须在高级卡上实测的结论。
- 全部当前公开 GPU 型号、架构、单机多卡上限、升级别名和调用示例见 `docs/modal.md`。`RTX-PRO-6000:8` 仅通过本机 SDK 参数解析，官方未明确八卡上限，也未实测分配，不能记为已可用。
- GPU 实验可复用本机已认证的 Modal CLI 与 Python SDK，先读 [Modal 调用与验证情况](docs/modal.md)，不另建认证或服务封装。
- 本机优先使用 PATH 中的 `modal`，否则使用 `~/.local/share/personal-dashboard/runtimes/modal-current/bin/modal`；凭据由 `~/.modal.toml` 或既有环境变量管理，不打印、不复制进项目。
- 2026-09-18 已验证 CLI 1.5.5 的应用列表与工作区月账单查询成功；这是只读验证，不代表 H100/B200 已分配或实验已完成。运行前重新查询当前状态。
- 通过 `modal run 实验文件.py::入口函数` 发起远端实验，镜像、GPU 型号、数量与超时写在实验定义中；硬件获取沿用上一节的确认规则，已有当前任务授权无需重复询问。
- 精确 H100 对照实验使用 `gpu="H100!"` 避免自动升级为 H200；B200 使用 `gpu="B200"`。实际设备型号必须在远端核实，软件版本与原始测量元数据一同记录。
- 本地只读汇总可复用相邻 `personal-dashboard/tools/modal_service.py`；它只查询状态和账单，不负责 GPU 实验执行。真实账号、账单、应用标识和凭据留在仓库外。

## Runpod 调用入口

- Runpod 是短期 GPU 调试的另一条已认证获取途径；先读 [Runpod 调用与费用控制](docs/runpod.md)，复用官方 CLI 或当前会话可调用的 Runpod MCP，不另建认证服务。
- 本机 CLI 为 `~/.local/bin/runpodctl`，认证复用 `~/.runpod/config.toml` 的 `apikey` 或 `RUNPOD_API_KEY`；不得打印或复制真实 Key、账号和账单到仓库。Codex 插件 OAuth 与 CLI 认证分开。
- 2026-09-18 已验证 CLI 2.14.0 的账户、Pods 和 GPU 目录只读查询成功；这不代表同机 8 卡 RTX PRO 6000 已可分配，也不代表 SSH、实验镜像或实际测量已验证。
- RTX PRO 6000 96GB Server Edition 的精确 GPU ID 与八卡命令见手册；不能用 MIG 分片、八个单卡 Pod 或其他型号替代同机八卡实验。
- 创建前沿用“高价值 GPU 与多卡实验”的计划、预计时间与费用约定，尽量准备好整套任务后一次测完；长期调试向用户申请常驻 4090 等资源。
- `--wait-timeout` 只限制 CLI 等待，超时后 Pod 仍保留；脚本超时也不会自动停止 Pod。失败后先查已有资源，不能盲目重建；回传结果后及时停止或终止本次资源，并核查存储费用。

## 技术栈与部署

| 用途 | 技术 |
| --- | --- |
| 页面与交互 | React + TypeScript |
| 开发与静态构建 | Vite |
| 样式 | 原生 CSS |
| 数学公式 | KaTeX |
| 代码高亮 | Shiki |
| 数据图表 | Apache ECharts，按需使用 |
| 托管与部署 | GitHub Pages + GitHub Actions |

- 前端开发遵循上述技术栈。
- KaTeX、Shiki 和 ECharts 在对应内容或功能需要时引入。

## GitHub Pages 部署

- 使用 GitHub Pages 托管静态构建产物。
- 部署工作流位于 `.github/workflows/pages.yml`，由 `main` 分支推送或手动运行触发。
- 仓库为 `luca-888/luca-888.github.io`，站点为 `https://luca-888.github.io/`；Vite 的 `base` 配置为 `/`。
- 静态资源引用使用 Vite 处理的路径或 `import.meta.env.BASE_URL`；更换部署路径时同步调整 `base`。
- 工作流安装锁定依赖并构建，部署 `dist/` 目录。
- 页面功能应适用于静态托管；涉及性能对比时，使用真实测量数据，正文仅注明测试显卡型号，演示数据须明确标注。

## 快速迭代与验证

- 开发阶段以迭代速度为主，优先通过 `npm run dev` 和热更新查看改动。
- 当前只面向桌面端，不考虑移动端布局，不进行移动端适配、截图或验证。
- 日常文案、样式和局部交互修改，默认不运行 build、完整类型检查或自动化测试，不例行重复截图和浏览器检查。
- 仅在出现错误、改动涉及关键逻辑或用户明确要求时，执行与当前问题直接相关的最小验证；完成后不额外扩大验证范围。
- 初始化前端工程后，提供 `npm run dev`、`npm run build` 和 `npm run preview` 命令，并提交依赖锁文件。
- 构建命令应包含 TypeScript 类型检查；发布前再运行完整构建，并检查桌面页面及 `/posts/rmsnorm/` 下的资源加载；旧 `/ml-portal/` 入口重定向至首页。

## 主页与文章目录

- 保留站点名称 `luca’s blog`，首页采用单列、平等排列的阅读卡片：所有文章使用一致的尺寸、标题字号、背景与图形大小，不单独突出最新文章。卡片包含标题、简短摘要与少量低饱和线条图形；保留搜索，不显示目录标题、日期、分类筛选、分类列、文章计数、列表结束说明或跳转箭头。使用白色与中性浅灰、细边框和留白，不加阴影。
- 文章信息统一维护在 `src/posts.ts`；正式发布填写真实日期和 `published` 状态。
- 每篇文章使用 `/posts/<slug>/` 稳定地址，在 Vite 中登记对应 HTML 构建入口。
- 主页与文章共享 `src/styles.css` 的颜色变量及 `src/SiteLayout.tsx` 的页头页脚；主页布局在 `src/site.css` 中维护，遵循统一的白底规范。
